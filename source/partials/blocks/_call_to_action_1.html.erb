<section class="bg-primary spacer-y-3">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col col-12 text-center">
        <span class="d-block mb-3 h2 text-white"><%= block.title %></span>
          <a class="btn btn-light btn-lg" href="<%= block.button.url %>"><%= block.button.title %></a>

        <span class="d-block mt-3 mt-3 text-white"><%= block.footer %></span>
      </div>
    </div>
  </div>
</section>
